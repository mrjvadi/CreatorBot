# وضعیت امکانات uploader-bot

علائم: ✅ کامل · 🟡 ناقص/وابسته به پلتفرم · ❌ خارج از دامنه

## زیرساخت
- ✅ بدون PostgreSQL؛ کل داده روی MongoDB با فیلتر `instance_id` (ایزولاسیون کامل هر ربات در رباتساز)
- ✅ کش تحویل کد روی Redis
- ✅ کدهای الفبا-عددی با پیشوند اختصاصی هر ربات
- ✅ بافر آلبوم اتمیک (بدون از‌دست‌رفتن فایل)
- ✅ پکیج‌بندی: `models / store / payment / util / tgbot`
- ✅ دریافت آپدیت تنظیمات/متن‌ها از NATS (`uploader.settings.<botID>`, `config.updated`)
- ✅ شمارش حد عضو با رویداد واقعی `membership.joined`
- ✅ کانال ذخیره‌سازی (DB Channel) — مقاوم در برابر تغییر توکن (fallback کپی از کانال)

## رسانه و تحویل
- ✅ آپلود همه انواع (عکس/ویدیو/صوت/گیف/استیکر/ویس/ویدیونوت/داکیومنت/آلبوم)
- ✅ کپشن + حفظ کامل فرمت/هایپرلینک (entities→HTML) + امضا با placeholder (`{code}`,`{count}`,`{downloads}`)
- ✅ منوی ویرایش کامل هر کد: قفل فوروارد، ضدفیلتر/حذف‌خودکار، رمز، محدودیت دانلود، اشتراک، سین/ری‌اکشن اجباری، لایک/دانلود/بازدید فیک، کاور ویدیو، ترتیب فایل‌ها، انتقال به پوشه
- ✅ لایک/دیسلایک واقعی (toggle) + گزارش + ارسال مجدد (روی آخرین پیام)
- ✅ هشدار حذف خودکار قابل‌تنظیم (متن/خاموش/پاک‌نشدن)
- ✅ تزریق تبلیغ در تحویل
- ✅ پیش‌نمایش در کانال
- ✅ نمایش لیستی و اسلایدی؛ جستجو با کد/کپشن/نوع رسانه
- ✅ پوشه و زیرپوشه

## قفل‌ها (شبیه تُرنج)
- ✅ حالت اجباری/اختیاری، نوع کانال/گروه/ربات/لینک
- ✅ حد عضو + غیرفعال‌سازی خودکار
- ✅ پنل کامل مدیریت قفل
- 🟡 گزارش لفت — وابسته به رویداد `membership.left` که پلتفرم (member-bot) منتشر نمی‌کند

## کاربران و دسترسی
- ✅ جستجوی کاربر، بلاک/آنبلاک، تغییر اشتراک، ریست دانلود
- ✅ صف تایید فایل‌های کاربران
- ✅ نقش‌ها و دسترسی‌های granular ادمین + پنل تنظیم

## همگانی
- ✅ کپی/فوروارد همگانی هر نوع پیام
- ✅ صف پس‌زمینه + محدودیت نرخ (FloodWait) + فاصله قابل‌تنظیم
- ✅ حذف خودکار بعد از X ساعت
- ✅ وضعیت زنده هر همگانی با کد (ارسال/بلاک/ارور/باقی‌مانده) + توقف + حذف‌فوری

## پرداخت و اشتراک
- ✅ بی‌نهایت پلن، ۵ روش پرداخت قابل‌فعال‌سازی
- ✅ درگاه واقعی زرین‌پال/زیبال (request+verify) + فعال‌سازی خودکار اشتراک
- ✅ کارت‌به‌کارت + ولت TON/TRON + مرچنت‌ها (تنظیمات)

## شخصی‌سازی
- ✅ همه متن‌ها و برچسب دکمه‌ها قابل‌تغییر (تنظیمات + NATS)
- ✅ دکمه‌های کاربری قابل‌فعال‌سازی + دکمه‌های شروع پیشرفته
- ✅ پنل ادمین کامل inline دسته‌بندی‌شده

## بکاپ
- ✅ بکاپ/ریستور فایل‌محور (JSON) کامل

## خارج از دامنه (این ربات فقط فایل→لینک تلگرام است، نه وب)
- ❌ شورت‌لینک/توکن درآمدزا، لینک تماشای آنلاین/استریم، دانلود وب

## ریفکتور باز
- 🟡 شکستن `tgbot` به زیرپکیج‌های فیچر (#35/#36) — نیازمند بازنویسی روی `core.App`؛ بهتر است مرحله‌ای و با کامپایلر انجام شود.
