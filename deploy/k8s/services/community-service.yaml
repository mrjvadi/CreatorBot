apiVersion: apps/v1
kind: Deployment
metadata:
  name: community-service
  namespace: creatorbot
  labels:
    app: community-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: community-service
  template:
    metadata:
      labels:
        app: community-service
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "9090"
    spec:
      containers:
      - name: community-service
        image: creatorbot/community-service:latest
        imagePullPolicy: Always
        env:
        - name: POSTGRES_DSN
          valueFrom: {secretKeyRef: {name: creatorbot-secrets, key: POSTGRES_DSN}}
        - name: NATS_URL
          valueFrom: {secretKeyRef: {name: creatorbot-secrets, key: NATS_URL}}
        - name: NATS_USERNAME
          valueFrom: {configMapKeyRef: {name: creatorbot-config, key: NATS_USERNAME}}
        - name: NATS_PASSWORD
          valueFrom: {secretKeyRef: {name: creatorbot-secrets, key: NATS_PASSWORD}}
        - name: REDIS_ADDR
          valueFrom: {secretKeyRef: {name: creatorbot-secrets, key: REDIS_ADDR}}
        - name: REDIS_PASSWORD
          valueFrom: {secretKeyRef: {name: creatorbot-secrets, key: REDIS_PASSWORD}}
        - name: ENCRYPTION_KEY
          valueFrom: {secretKeyRef: {name: creatorbot-secrets, key: ENCRYPTION_KEY}}
        - name: MONGO_URI
          valueFrom: {secretKeyRef: {name: creatorbot-secrets, key: MONGO_URI}}
        resources:
          requests:
            memory: "64Mi"
            cpu: "50m"
          limits:
            memory: "256Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8093
          initialDelaySeconds: 10
          periodSeconds: 30
        readinessProbe:
          httpGet:
            path: /health
            port: 8093
          initialDelaySeconds: 5
          periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: community-service
  namespace: creatorbot
spec:
  selector:
    app: community-service
  ports:
  - port: 8093
    targetPort: 8093
  type: ClusterIP
