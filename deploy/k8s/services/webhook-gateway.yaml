apiVersion: apps/v1
kind: Deployment
metadata:
  name: webhook-gateway
  namespace: creatorbot
  labels:
    app: webhook-gateway
spec:
  replicas: 2
  selector:
    matchLabels:
      app: webhook-gateway
  template:
    metadata:
      labels:
        app: webhook-gateway
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "9090"
    spec:
      containers:
      - name: webhook-gateway
        image: creatorbot/webhook-gateway:latest
        imagePullPolicy: Always
        env:
        - name: POSTGRES_DSN
          valueFrom: {secretKeyRef: {name: creatorbot-secrets, key: POSTGRES_DSN}}
        - name: NATS_URL
          valueFrom: {secretKeyRef: {name: creatorbot-secrets, key: NATS_URL}}
        - name: NATS_USERNAME
          valueFrom: {configMapKeyRef: {name: creatorbot-config, key: NATS_USERNAME}}
        - name: NATS_PASSWORD
          valueFrom: {secretKeyRef: {name: creatorbot-secrets, key: NATS_PASSWORD}}
        - name: REDIS_ADDR
          valueFrom: {secretKeyRef: {name: creatorbot-secrets, key: REDIS_ADDR}}
        - name: REDIS_PASSWORD
          valueFrom: {secretKeyRef: {name: creatorbot-secrets, key: REDIS_PASSWORD}}
        - name: ENCRYPTION_KEY
          valueFrom: {secretKeyRef: {name: creatorbot-secrets, key: ENCRYPTION_KEY}}
        - name: INTERNAL_KEY
          valueFrom: {secretKeyRef: {name: creatorbot-secrets, key: INTERNAL_KEY}}
        - name: BOTMANAGER_TOKEN
          valueFrom: {secretKeyRef: {name: creatorbot-secrets, key: BOTMANAGER_TOKEN}}
        resources:
          requests:
            memory: "64Mi"
            cpu: "50m"
          limits:
            memory: "256Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8090
          initialDelaySeconds: 10
          periodSeconds: 30
        readinessProbe:
          httpGet:
            path: /health
            port: 8090
          initialDelaySeconds: 5
          periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: webhook-gateway
  namespace: creatorbot
spec:
  selector:
    app: webhook-gateway
  ports:
  - port: 8090
    targetPort: 8090
  type: ClusterIP
