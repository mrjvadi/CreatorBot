apiVersion: apps/v1
kind: Deployment
metadata:
  name: apimanager
  namespace: creatorbot
  labels:
    app: apimanager
spec:
  replicas: 2
  selector:
    matchLabels:
      app: apimanager
  template:
    metadata:
      labels:
        app: apimanager
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "9090"
    spec:
      containers:
      - name: apimanager
        image: creatorbot/apimanager:latest
        imagePullPolicy: Always
        env:
        - name: POSTGRES_DSN
          valueFrom: {secretKeyRef: {name: creatorbot-secrets, key: POSTGRES_DSN}}
        - name: NATS_URL
          valueFrom: {secretKeyRef: {name: creatorbot-secrets, key: NATS_URL}}
        - name: NATS_USERNAME
          valueFrom: {configMapKeyRef: {name: creatorbot-config, key: NATS_USERNAME}}
        - name: NATS_PASSWORD
          valueFrom: {secretKeyRef: {name: creatorbot-secrets, key: NATS_PASSWORD}}
        - name: REDIS_ADDR
          valueFrom: {secretKeyRef: {name: creatorbot-secrets, key: REDIS_ADDR}}
        - name: REDIS_PASSWORD
          valueFrom: {secretKeyRef: {name: creatorbot-secrets, key: REDIS_PASSWORD}}
        - name: ENCRYPTION_KEY
          valueFrom: {secretKeyRef: {name: creatorbot-secrets, key: ENCRYPTION_KEY}}
        - name: JWT_ACCESS_SECRET
          valueFrom: {secretKeyRef: {name: creatorbot-secrets, key: JWT_ACCESS_SECRET}}
        - name: INTERNAL_KEY
          valueFrom: {secretKeyRef: {name: creatorbot-secrets, key: INTERNAL_KEY}}
        resources:
          requests:
            memory: "64Mi"
            cpu: "50m"
          limits:
            memory: "256Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 10
          periodSeconds: 30
        readinessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: apimanager
  namespace: creatorbot
spec:
  selector:
    app: apimanager
  ports:
  - port: 8080
    targetPort: 8080
  type: ClusterIP
