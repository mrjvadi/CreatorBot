# env پایه‌ی سرور — secret واقعی، روی هر سرور جدا ساخته می‌شود (رجوع
# agentmanager/.env کلید BOT_BASE_ENV_FILE). بقیه‌ی .env ها فعلاً در git
# هستند (تصمیم قدیمی — CLAUDE.md: باید rotate شوند)؛ حداقل این یکی از
# روز اول بیرون می‌ماند.
agentmanager/botenv.env
agentmanager/botenv/

# خروجی اجرای محلی run.sh
.logs/
.run.pids
tools/e2e-provision/e2e-provision
